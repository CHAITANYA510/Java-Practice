version: '4.2.0'

services:

  currency-exchange:
    image: chaitanya510/ms.v1-currency-exchange-service:0.0.1-SNAPSHOT
    mem_limit: 700m
    ports:
      - "8000:8000"
    networks:
      - currency-network

networks:
  currency-network: